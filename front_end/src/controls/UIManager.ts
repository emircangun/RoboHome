import { SceneManager } from '../scene/SceneManager';

export class UIManager {
  constructor(_sceneManager: SceneManager) {
    // UI functionality removed - only chat and 3D window remain
    // UIManager kept for compatibility but no longer manages any UI
  }
}
